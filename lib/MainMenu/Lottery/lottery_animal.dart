import 'package:flutter/material.dart';

class AnimalLottery extends StatefulWidget {
  const AnimalLottery({Key? key}) : super(key: key);

  @override
  State<AnimalLottery> createState() => _AnimalLotteryState();
}

class _AnimalLotteryState extends State<AnimalLottery> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(''),
    );
  }
}
